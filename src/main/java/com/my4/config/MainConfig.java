package com.my4.config;/* @author hzy @DESCRIPTION ${DESCRIPTION} @create 2020/5/6*/import com.my.Person;import org.springframework.context.annotation.Bean;import org.springframework.context.annotation.Conditional;import org.springframework.context.annotation.Configuration;@Configurationpublic class MainConfig {    // 给容器中注入一个bean，类型为该方法的返回值类型，默认创建方式是单实例    @Bean    public Person person(){        System.out.println("向容器中注入对象person");        return new Person("person",24);    }    //指示组件注册需要满足的条件，需要一个条件过滤类来进行处理    @Conditional(WinCondition.class)    @Bean    public Person hzy(){        System.out.println("向容器中注入对象hzy");        return new Person("hzy",24);    }    @Conditional(LinCondition.class)    @Bean    public Person zyy(){        System.out.println("向容器中注入对象zyy");        return new Person("zyy",24);    }}